# Root pip requirements for venv support.
# This file references the EMRDM Phase 2 requirements list.
# Adjust CUDA-specific torch/torchvision/torchaudio specifiers in
# phase2_emrdm/requirements.txt if you are on macOS or a CPU-only system.

-r phase2_emrdm/requirements.txt

# python -m venv .venv
# source .venv/bin/activate
# python -m pip install --upgrade pip
# python -m pip install -r requirements.txt


# DTU HPC environment - torch 2.8.0+cu128, Python 3.9
pytorch-lightning==2.3.0
omegaconf==2.3.0
einops
wandb==0.17.8
natsort==8.4.0
dctorch==0.1.2
rasterio==1.3.11
tifffile==2024.7.24
scipy==1.14.0
opencv-python==4.10.0.84
lpips==0.1.4
s2cloudless==1.7.2
albumentations==1.4.10
natten==0.21.1+torch280cu128
